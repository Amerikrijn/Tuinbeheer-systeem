name: 🧪 Test Workflow

on:
  push:
    branches: [main, develop]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 📝 Test Step
        run: |
          echo "✅ GitHub Actions is working!"
          echo "🚀 This confirms workflows can be detected and run"
          echo "📁 Current directory: $(pwd)"
          echo "🔍 Workflow files:"
          ls -la .github/workflows/